version: '3.5'

services:
  nginx:
    build: nginx/
    networks:
      - default
      - nginx-bridge
    volumes:
      - "./html:/usr/share/nginx/html/:ro"
    environment:
       VIRTUAL_HOST: "tsiamsi.ithuan.tw"
       LETSENCRYPT_HOST: "tsiamsi.ithuan.tw"
       LETSENCRYPT_EMAIL: "ithuan@ithuan.tw"
    restart: always

networks:
  nginx-bridge:
    external: true
